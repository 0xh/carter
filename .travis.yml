language: php

php:
#  - 7.0
  - 7.1

before_script:
  - composer self-update
  - composer install --no-interaction

script:
  - mkdir -p build/logs
  - vendor/bin/phpunit

after_success:
  - travis_retry vendor/bin/coveralls